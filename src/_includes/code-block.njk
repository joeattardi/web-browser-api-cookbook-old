{% macro renderFiles(collection, basedir) %}
  {% for file in collection %}
    {% include basedir + "/" + file.filename %}
  {% endfor %}
{% endmacro %}

{% macro codeBlock(language, code) %}
  <div class="code-block">
    <h4>
      <div class="language {{ language }}">{{ language }}</div>
    </h4>
    {% highlight language %}
    {{ code | safe }}
    {% endhighlight %}
  </div>
{% endmacro %}